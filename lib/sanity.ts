/**
 * Sanity client for CMS content (blog, etc.).
 * Set NEXT_PUBLIC_SANITY_PROJECT_ID and NEXT_PUBLIC_SANITY_DATASET in .env.local
 * Install: npm install next-sanity
 */
// import { createClient } from 'next-sanity';

// const projectId = process.env.NEXT_PUBLIC_SANITY_PROJECT_ID ?? '';
// const dataset = process.env.NEXT_PUBLIC_SANITY_DATASET ?? 'production';
// export const sanityClient = createClient({ projectId, dataset });

export const sanityClient = null;
